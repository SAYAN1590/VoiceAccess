# Copy this file to .streamlit/secrets.toml and add your real key.
OPENAI_API_KEY = "your-api-key-here"

# Optional: needed only when the virtual environment cannot discover FFmpeg.
# FFMPEG_BINARY = "C:/full/path/to/ffmpeg.exe"
# FFPROBE_BINARY = "C:/full/path/to/ffprobe.exe"
